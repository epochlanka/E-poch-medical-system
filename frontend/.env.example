# Optional overrides. Leave blank for local/LAN use; the app automatically uses the hostname
# through which the browser opened it. Set explicit public HTTPS URLs for production hosting.
VITE_API_URL=
VITE_ADMIN_APP_URL=
VITE_DOCTOR_APP_URL=
VITE_RECEPTIONIST_APP_URL=
VITE_PHARMACIST_APP_URL=
