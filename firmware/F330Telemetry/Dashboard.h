#pragma once
#include <pgmspace.h>
static const char DASHBOARD_HTML[] PROGMEM = R"F330HTML(
<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>F330 Flight Deck</title><style>
:root{color-scheme:dark;--bg:#090d11;--panel:#111920;--line:#2b3943;--muted:#8ca0ae;--cyan:#69e4dc;--amber:#ffd56a;--red:#ff7f74}*{box-sizing:border-box}body{margin:0;background:radial-gradient(ellipse at top,#1a2732,var(--bg) 65%);color:#e5edf1;font:14px system-ui,sans-serif}main{max-width:1450px;margin:auto;padding:22px}header{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-bottom:20px}h1{font-size:27px;letter-spacing:4px;margin:5px 0}h2{font-size:12px;letter-spacing:2px;text-transform:uppercase;font-weight:600;margin:0 0 15px}.eyebrow{font-size:10px;letter-spacing:3px;color:var(--cyan)}.muted,small{color:var(--muted)}.bad{color:var(--red)!important}.warn{color:var(--amber)!important}.good{color:var(--cyan)!important}.pill{border:1px solid var(--line);background:#0c141a;padding:8px 12px;border-radius:4px;font:12px ui-monospace,monospace}#annunciator{padding:12px 16px;border:1px solid var(--line);border-left:3px solid var(--amber);margin-bottom:16px;background:#111920}.deck{display:grid;grid-template-columns:235px minmax(0,1fr) 235px;gap:14px;align-items:stretch}.panel{min-width:0;background:linear-gradient(145deg,#17212a,#10171d);border:1px solid var(--line);border-radius:8px;padding:18px;box-shadow:0 6px 20px #0004}.instrument{border:2px solid #38434d;padding:8px;background:#060a0d;position:relative;border-radius:12px;box-shadow:inset 0 0 0 5px #141b20}.instrument:before,.instrument:after{content:'+';position:absolute;color:#52606a;top:9px;font:16px monospace}.instrument:before{left:12px}.instrument:after{right:12px}canvas{display:block;width:100%;aspect-ratio:1.13;background:#070c10;border-radius:6px}.readout{padding:14px 0;border-bottom:1px solid var(--line)}.readout:first-of-type{padding-top:0}.readout b{display:block;font:30px ui-monospace,monospace;margin:6px 0;letter-spacing:-1px}.readout label{font-size:10px;letter-spacing:1.5px;color:var(--muted)}.unit{font-size:12px;color:var(--muted)}.status{font:11px ui-monospace,monospace;margin-top:8px}.triplet{display:flex;justify-content:space-around;text-align:center;padding:14px 0 2px;font:13px ui-monospace,monospace}.triplet b{color:var(--cyan);font-size:17px}.lower{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px}.full{grid-column:1/-1}.toolbar{display:flex;justify-content:space-between;gap:12px;align-items:center}.toolbar h2{margin:0}select{background:#0c1217;color:var(--cyan);border:1px solid var(--line);border-radius:4px;padding:7px;font:11px monospace}.sticks{display:flex;gap:25px;justify-content:space-around;margin:20px 0}.stick-wrap{width:43%;max-width:200px;text-align:center}.stick{position:relative;aspect-ratio:1;border:1px solid #425361;border-radius:50%;margin:10px 0;background:linear-gradient(90deg,transparent 49.6%,#334654 50%,transparent 50.4%),linear-gradient(transparent 49.6%,#334654 50%,transparent 50.4%),radial-gradient(#1c2e3b,#0c1319)}.dot{position:absolute;left:50%;top:50%;width:13px;height:13px;border-radius:50%;background:var(--cyan);transform:translate(-50%,-50%);box-shadow:0 0 12px #69e4dc77}.dim{opacity:.3}.kv{display:flex;justify-content:space-between;gap:10px;padding:9px 0;border-bottom:1px solid var(--line);font:12px ui-monospace,monospace}.kv span{color:var(--muted)}.kv b{font-weight:500;text-align:right}.rates{display:flex;gap:20px;flex-wrap:wrap;margin:12px 0}.rates b{font:22px ui-monospace,monospace;color:var(--cyan)}.rates small{display:block;font-size:10px;margin-top:3px}.scroll{overflow-x:auto}table{width:100%;border-collapse:collapse;white-space:nowrap;font:12px ui-monospace,monospace}td,th{padding:12px 9px;border-bottom:1px solid var(--line);text-align:right}td:first-child,th:first-child{text-align:left}th{font-size:10px;font-weight:500;color:var(--muted)}.note{font-size:12px;line-height:1.6;color:var(--muted);margin:12px 0 0}.stale{border-color:#956c37}footer{padding:18px 0;color:var(--muted);font-size:11px;line-height:1.7}.heading{display:flex;justify-content:space-between;margin:8px 15px 10px;font:11px ui-monospace,monospace}.heading b{color:var(--cyan)}@media(max-width:1000px){.deck{grid-template-columns:175px minmax(0,1fr) 175px}.panel{padding:13px}.readout b{font-size:25px}}@media(max-width:760px){main{padding:12px}.deck{grid-template-columns:1fr 1fr}.instrument{grid-column:1/-1;grid-row:1}.lower{grid-template-columns:1fr}.full{grid-column:auto}h1{font-size:21px}header{gap:8px}.pill{padding:7px;font-size:10px}.panel{padding:15px}.side{display:grid;grid-template-columns:1fr}.readout b{font-size:24px}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto}}
</style></head><body><main>
<header><div><div class="eyebrow">RADIO MANAGEMENT / PICO 2</div><h1>F330 FLIGHT DECK</h1><small>Live Madflight instruments · read-only</small></div><div class="pill" id="connection">CONNECTING</div></header>
<div id="annunciator" role="status">Waiting for bridge data…</div>
<div class="deck"><section class="panel side"><h2>Navigation</h2><div class="readout"><label>GROUND SPEED · KM/H</label><b id="speed">—</b><span class="unit">GPS · not airspeed</span></div><div class="readout"><label>YAW / HEADING · DEG</label><b id="heading">—</b><span class="unit">FC yaw · may drift without a heading reference</span></div><div class="readout"><label>GPS SATELLITES</label><b id="sats">—</b><span class="unit" id="gpsState">NO DATA</span></div><div class="status" id="gpsAge">GPS NOT RECEIVED</div></section>
<section class="instrument"><div class="heading"><span>PRIMARY FLIGHT DISPLAY</span><b id="flightMode">—</b></div><canvas id="horizon" width="720" height="640" role="img" aria-label="Artificial horizon: waiting for FC attitude"></canvas><div class="triplet"><span>ROLL<br><b id="roll">—</b></span><span>PITCH<br><b id="pitch">—</b></span><span>YAW<br><b id="yaw">—</b></span></div><div class="heading"><span id="attAge">ATTITUDE NOT RECEIVED</span><span>INTERPOLATED DISPLAY</span></div></section>
<section class="panel side"><h2>Vertical / power</h2><div class="readout"><label>FC ALTITUDE · M</label><b id="height">—</b><span class="unit" id="altAge">NO DATA</span></div><div class="readout"><label>VERTICAL SPEED · M/S</label><b id="vs">—</b><span class="unit">+ climb / − descent</span></div><div class="readout"><label>BATTERY · V / A</label><b id="battery">—</b><span class="unit" id="consumed">Battery sensor disabled in supplied config</span></div><div class="status" id="batteryAge">BATTERY NOT RECEIVED</div></section></div>
<div class="lower"><section class="panel"><div class="toolbar"><h2>Control inputs · Mode 2</h2><select id="stickSource" aria-label="Stick data source"><option value="output">Processed output</option><option value="raw">Raw PWM</option></select></div><div class="sticks"><div class="stick-wrap"><small>THROTTLE / YAW</small><div class="stick"><div class="dot" id="leftDot"></div></div><small id="leftValues">—</small></div><div class="stick-wrap"><small>PITCH / ROLL</small><div class="stick"><div class="dot" id="rightDot"></div></div><small id="rightValues">—</small></div></div><div class="kv"><span>CH6 arm request</span><b id="armRequest">—</b></div><div class="kv"><span>FC-reported arming</span><b id="fcArmed">UNKNOWN</b></div><div class="kv"><span>Throttle sent to FC</span><b id="throttle">—</b></div><p class="note" id="armHelp">Use the corrected Madflight config: CH6 arms, CH5 selects mode. Arming needs zero FC throttle and a low → high switch transition.</p></section>
<section class="panel"><h2>Link health</h2><div class="rates"><div><b id="uiRate">—</b><small>DASHBOARD HZ</small></div><div><b id="attRate">—</b><small>FC ATTITUDE HZ</small></div><div><b id="rcRate">—</b><small>RC TX HZ</small></div></div><div class="kv"><span>Valid CRSF / CRC rejects</span><b id="frames">—</b></div><div class="kv"><span>UART framing / overflow</span><b id="uart">—</b></div><div class="kv"><span>Noise bytes / bad lengths</span><b id="noise">—</b></div><div class="kv"><span>RC skips / late cycles</span><b id="timing">—</b></div><div class="kv"><span>Receiver / snapshot age</span><b id="rxAge">—</b></div><div class="kv"><span>Free heap / radio work max</span><b id="heap">—</b></div><p class="note" id="diagnosis">Waiting for diagnostics…</p></section>
<section class="panel full"><h2>Channel inspector</h2><div class="scroll"><table><thead><tr><th>Channel</th><th>Raw PWM µs</th><th>Filtered µs</th><th>Output µs</th><th>CRSF 11-bit</th><th>Wire µs</th><th>Pulse age</th></tr></thead><tbody id="channels"></tbody></table></div><p class="note">Calibration: 1150–1800 µs → 1000–2000 µs. The 800 µs failsafe request encodes as ~988 µs. CH3 raw retains the last accepted pulse. Moving dots show radio input, not cockpit controls.</p></section>
<section class="panel full"><h2>Position & source notes</h2><div id="coords" class="note">GPS coordinates: —</div><p class="note">Stock Madflight attitude/mode/altitude: about 10 Hz; battery/GPS: about 1 Hz. The persistent dashboard stream targets 20 Hz. Smooth rendering adds no new sensor samples. Reported battery percentage is a Madflight placeholder, so it is not used as a fuel gauge. No blackbox recording.</p></section></div><footer>F330 Wi-Fi · 192.168.4.1 · CRSF 420000 baud · Verify attitude direction, channel mapping and failsafe with propellers removed before flight.</footer>
</main><script>
'use strict';
const $=id=>document.getElementById(id), names=['CH1 · ROLL','CH2 · PITCH','CH3 · THROTTLE','CH4 · YAW','CH5 · MODE','CH6 · ARM'];
const text=(id,v)=>{const el=$(id);if(el.textContent!==String(v))el.textContent=v};
let latest=null,lastArrival=0,transport='CONNECTING',streamUp=false;
let rateAt=performance.now(),messages=0,baseAtt=null,baseTx=null,shown={roll:0,pitch:0,yaw:0},lastDraw=0,epoch=null;
const cells=names.map(name=>{const row=document.createElement('tr');const cols=Array.from({length:7},()=>{const c=document.createElement('td');row.append(c);return c});cols[0].textContent=name;$('channels').append(row);return cols});
function elapsed(){return Math.max(0,performance.now()-lastArrival)}
function effectiveAge(t){return !t||t.age===null?Infinity:t.age+elapsed()}
function fresh(t,limit=1500){return effectiveAge(t)<=limit&&latest&&latest.snapshotAge+elapsed()<500}
function ageText(t,label){const a=effectiveAge(t);return !Number.isFinite(a)?`${label} NOT RECEIVED`:a>1500?`${label} STALE · ${(a/1000).toFixed(1)}s`:`${label} ${Math.round(a)}ms`}
function value(t,key,decimals=1){return !t||t.age===null?'—':Number(t[key]).toFixed(decimals)}
function wrap(a){return ((a+180)%360+360)%360-180}
function heading(a){return ((a%360)+360)%360}
function sticks(d){const src=$('stickSource').value;const scale=v=>Math.max(0,Math.min(100,src==='raw'?(v-1150)/6.5:(v-1000)/10));
for(const [id,x,y] of [['leftDot',3,2],['rightDot',0,1]]){const dot=$(id);dot.style.left=`${8+.84*scale(d.channels[x][src])}%`;dot.style.top=`${92-.84*scale(d.channels[y][src])}%`;dot.classList.toggle('dim',d.channels[x].age===null||d.channels[y].age===null||d.channels[x].age+elapsed()>500||d.channels[y].age+elapsed()>500)}
text('leftValues',`T ${d.channels[2][src]} / Y ${d.channels[3][src]} µs`);text('rightValues',`P ${d.channels[1][src]} / R ${d.channels[0][src]} µs`)}
$('stickSource').addEventListener('change',()=>{if(latest)sticks(latest)});
function accept(d,source){if(!d.channels||!d.attitude||!d.stats)throw Error('Incomplete state');
if(epoch!==null&&d.uptime<epoch){baseAtt=null;baseTx=null;messages=0;rateAt=performance.now();}epoch=d.uptime;
const first=!latest;latest=d;lastArrival=performance.now();transport=source;messages++;
if(first&&d.attitude.age!==null)shown={...d.attitude};
const s=d.stats,now=performance.now();if(baseAtt===null){baseAtt=s.attitudeFrames;baseTx=s.tx}
if(now-rateAt>=1000){const seconds=(now-rateAt)/1000;text('uiRate',(messages/seconds).toFixed(1));text('attRate',Math.max(0,(s.attitudeFrames-baseAtt)/seconds).toFixed(1));text('rcRate',Math.max(0,(s.tx-baseTx)/seconds).toFixed(0));messages=0;rateAt=now;baseAtt=s.attitudeFrames;baseTx=s.tx}
text('heading',d.attitude.age===null?'—':heading(d.attitude.yaw).toFixed(0).padStart(3,'0')+'°');for(const k of ['roll','pitch','yaw'])text(k,value(d.attitude,k)+'°');
text('speed',value(d.gps,'speed'));text('sats',value(d.gps,'sats',0));text('gpsState',d.gps.age===null?'NO DATA':d.gps.sats?'SATELLITES RECEIVED':'NO SATELLITES');
text('height',value(d.altitude,'height'));text('vs',value(d.altitude,'vs',2));text('battery',d.battery.age===null?'—':`${value(d.battery,'voltage')} / ${value(d.battery,'current')}`);text('consumed',d.battery.age===null?'Battery sensor disabled in supplied config':`${d.battery.mah} mAh reported · sensor disabled in supplied config`);
text('flightMode',d.mode.age===null?'—':d.mode.text||'—');
text('throttle',`${d.channels[2].wireUs.toFixed(1)} µs (requested ${d.channels[2].output})`);
text('frames',`${s.valid} / ${s.crcErrors}`);text('uart',`${s.uartFrameErrors} / ${s.uartOverflows}`);text('noise',`${s.noiseBytes} / ${s.badLengths}`);text('timing',`${s.skipped} / ${s.late}`);text('heap',`${Math.round(s.heap/1024)} KiB / ${s.maxCycleUs} µs`);
text('coords',d.gps.age===null?'GPS coordinates: —':`LAT ${d.gps.lat.toFixed(7)} · LON ${d.gps.lon.toFixed(7)} · GPS ALT ${d.gps.height.toFixed(1)} m · COURSE ${d.gps.course.toFixed(1)}°`);
text('diagnosis',s.uartFrameErrors||s.uartOverflows?'UART errors detected. Confirm 420000 baud at both ends, common ground, correct RX pin, and bbx_gizmo NONE. Counters are cumulative since boot.':s.crcErrors||s.noiseBytes?'Rejected/noise bytes detected. Confirm serial bus 1 is dedicated to CRSF; OpenLog must be disabled. Counters are cumulative since boot.':'No UART/CRC errors counted. Stock FC attitude updates around 10 Hz; stream target is 20 Hz.');
d.channels.forEach((c,i)=>{[names[i],c.age===null?'—':c.raw,c.filtered,c.output,c.crsf,c.wireUs.toFixed(1),c.age===null?'NO PULSE':`${c.age} ms`].forEach((v,j)=>{if(cells[i][j].textContent!==String(v))cells[i][j].textContent=v});cells[i][0].parentElement.classList.toggle('warn',c.age===null||c.age>500)});sticks(d);status();}
function status(){if(!latest)return;const d=latest,lost=elapsed()>1000,radioStale=d.snapshotAge+elapsed()>500;
text('connection',lost?'DISCONNECTED':transport);$('connection').className=`pill ${lost?'bad':'good'}`;
const notice=lost?'CONNECTION LOST · instruments frozen':radioStale?'RADIO SNAPSHOT STALE · channel state unknown':d.timeout?'RECEIVER TIMEOUT · throttle minimum / arm request forced LOW':d.receiverFailsafe?'RECEIVER FAILSAFE · throttle below 1100 µs / arm request forced LOW':!fresh(d.attitude)?'FC TELEMETRY STALE · check Pico UART config and wiring':'RADIO ACTIVE · live flight telemetry';
text('annunciator',notice);$('annunciator').className=lost?'bad':radioStale||d.timeout||d.receiverFailsafe||!fresh(d.attitude)?'warn':'good';
text('armRequest',radioStale?'UNKNOWN':d.channels[5].output>1960?'HIGH · CH6':'LOW · CH6');text('fcArmed',fresh(d.mode)?d.mode.text.startsWith('*')?'ARMED':'DISARMED':'UNKNOWN · no fresh mode');
text('armHelp',d.timeout||d.receiverFailsafe?'Bridge failsafe is forcing CH6 LOW. Verify receiver pulses and low-throttle calibration first.':d.channels[5].output>1960&&!d.mode.text.startsWith('*')?'Arm requested but not confirmed. Check FC rcl_arm_ch=6, zero FC throttle, then toggle arm LOW → HIGH. Telemetry does not include the FC’s full pre-arm failure reason.':'Corrected config uses CH6 for arm and CH5 for flight mode. At zero FC throttle, toggle CH6 LOW → HIGH.');
for(const [key,id,label] of [['attitude','attAge','ATT'],['altitude','altAge','ALT'],['gps','gpsAge','GPS'],['battery','batteryAge','BAT']]){text(id,ageText(d[key],label));$(id).className=`status ${fresh(d[key],key==='gps'||key==='battery'?3000:1500)?'good':'warn'}`}
text('rxAge',`${d.rxAge===null?'—':Math.round(d.rxAge+elapsed())+' ms'} / ${Math.round(d.snapshotAge+elapsed())} ms`);
if(lost){text('uiRate','0.0');text('attRate','—');text('rcRate','—')} }
const canvas=$('horizon'),ctx=canvas.getContext('2d');
function line(x1,y1,x2,y2,color='#fff',width=2){ctx.strokeStyle=color;ctx.lineWidth=width;ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke()}
function label(t,x,y,size=16,color='#fff'){ctx.fillStyle=color;ctx.font=`${size}px ui-monospace,monospace`;ctx.textAlign='center';ctx.fillText(t,x,y)}
function draw(now){const dt=Math.min(.1,(now-lastDraw)/1000||.016);lastDraw=now;const ok=latest&&fresh(latest.attitude);if(ok){const a=latest.attitude;const blend=matchMedia('(prefers-reduced-motion: reduce)').matches?1:1-Math.exp(-dt/0.055);shown.roll+=wrap(a.roll-shown.roll)*blend;shown.pitch+=(a.pitch-shown.pitch)*blend;shown.yaw+=wrap(a.yaw-shown.yaw)*blend}
ctx.clearRect(0,0,720,640);ctx.fillStyle='#080e13';ctx.fillRect(0,0,720,640);
// Attitude field: positive nose-up lowers the horizon; positive right bank
// rotates the outside world counterclockwise around the fixed aircraft symbol.
ctx.save();ctx.beginPath();ctx.rect(55,70,610,485);ctx.clip();ctx.translate(360,305);ctx.rotate(-shown.roll*Math.PI/180);ctx.translate(0,shown.pitch*5);
ctx.fillStyle='#1f6ba0';ctx.fillRect(-1200,-1500,2400,1500);ctx.fillStyle='#795437';ctx.fillRect(-1200,0,2400,1500);line(-1200,0,1200,0,'#f8fbff',3);
for(let p=-90;p<=90;p+=5){if(!p)continue;const y=-p*5,w=p%10===0?65:32;line(-w,y,w,y,'#fff',p%10===0?2:1);if(p%10===0){label(Math.abs(p),-w-25,y+5,15);label(Math.abs(p),w+25,y+5,15)}}ctx.restore();
// Fixed bank scale and moving bank pointer.
ctx.save();ctx.translate(360,305);for(const deg of [-60,-45,-30,-20,-10,0,10,20,30,45,60]){const a=(deg-90)*Math.PI/180,r=219,len=deg%30===0?19:10;line(Math.cos(a)*r,Math.sin(a)*r,Math.cos(a)*(r-len),Math.sin(a)*(r-len),'#fff',2)}ctx.rotate(-shown.roll*Math.PI/180);ctx.fillStyle='#fff';ctx.beginPath();ctx.moveTo(0,-205);ctx.lineTo(-9,-189);ctx.lineTo(9,-189);ctx.closePath();ctx.fill();ctx.restore();
// Fixed aircraft wings and center reference.
line(235,305,315,305,'#ffd56a',6);line(315,305,315,320,'#ffd56a',6);line(405,305,485,305,'#ffd56a',6);line(405,305,405,320,'#ffd56a',6);ctx.strokeStyle='#ffd56a';ctx.lineWidth=3;ctx.beginPath();ctx.arc(360,305,6,0,2*Math.PI);ctx.stroke();
ctx.fillStyle='#0b131de8';ctx.fillRect(55,493,610,62);const hdg=heading(shown.yaw);for(let offset=-60;offset<=60;offset+=5){const degree=Math.round(hdg/5)*5+offset,x=360+wrap(degree-hdg)*4;if(x<65||x>655)continue;line(x,505,x,degree%10===0?519:513,'#8aaebb',1);if(degree%30===0)label(({0:'N',90:'E',180:'S',270:'W'})[heading(degree)]||String(heading(degree)).padStart(3,'0'),x,543,16)}
ctx.fillStyle='#080e13';ctx.fillRect(316,483,88,34);ctx.strokeStyle='#69e4dc';ctx.strokeRect(316,483,88,34);label(ok?heading(shown.yaw).toFixed(0).padStart(3,'0')+'°':'---',360,507,22,'#69e4dc');
label('ROLL',100,40,12,'#91a8b9');label(latest?value(latest.attitude,'roll')+'°':'—',100,61,18);label('PITCH',620,40,12,'#91a8b9');label(latest?value(latest.attitude,'pitch')+'°':'—',620,61,18);
label('ATTITUDE / FC YAW',360,596,12,'#7d94a4');
if(!ok){ctx.fillStyle='#0b111acc';ctx.fillRect(55,70,610,485);label(!latest||latest.attitude.age===null?'NO ATTITUDE DATA':'ATTITUDE STALE',360,280,27,'#ffb26c');label('Check CRSF link · instrument frozen',360,315,15,'#e0e6ec')}
canvas.setAttribute('aria-label',ok?`Artificial horizon: roll ${shown.roll.toFixed(1)} degrees, pitch ${shown.pitch.toFixed(1)} degrees`:'Artificial horizon: attitude unavailable or stale');requestAnimationFrame(draw)}
requestAnimationFrame(draw);setInterval(status,200);
let events=null;function connectStream(){events=new EventSource(`http://${location.hostname}:81/events`);events.onmessage=e=>{try{accept(JSON.parse(e.data),'LIVE STREAM');streamUp=true}catch{streamUp=false}};events.onerror=()=>{streamUp=false};}
async function fallback(){if(!streamUp||elapsed()>500){const c=new AbortController(),timer=setTimeout(()=>c.abort(),1000);try{const r=await fetch('/api/state',{cache:'no-store',signal:c.signal});if(r.ok){const d=await r.json();if(!streamUp||elapsed()>500)accept(d,'HTTP FALLBACK')}}catch{}finally{clearTimeout(timer)}}setTimeout(fallback,250)}
connectStream();fallback();
</script></body></html>
)F330HTML";
